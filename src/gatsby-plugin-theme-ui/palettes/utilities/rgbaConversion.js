// import colors from '../../colors'



// function hexToRGBA(hex, opacity) {
//     return 'rgba(' + (hex = hex.replace('#', '')).match(new RegExp('(.{' + hex.length/3 + '})', 'g')).map(function(l) { return parseInt(hex.length%2 ? l+l : l, 16) }).concat(isFinite(opacity) ? opacity : 1).join(',') + ')';
// }

// test = Object.call(colors.primary)
// let test = colors.primary

// export default {
//     primaryRGBA: {
//         '100': 'test'
        // '100': hexToRGBA('#000'),
// '100': hexToRGBA(Object.toString(colors.primary)),

// }
// }
// document.getElementById('color').style.setProperty('background-color', test)

